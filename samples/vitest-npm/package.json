{
    "name": "vitest-npm",
    "version": "1.0.0",
    "description": "Minimal Vitest project (npm) for Automaspec integration",
    "type": "module",
    "scripts": {
        "test": "vitest",
        "test:run": "vitest run",
        "test:json": "vitest run --reporter=json --outputFile=test-results.json"
    },
    "devDependencies": {
        "vitest": "^3.0.0"
    }
}
