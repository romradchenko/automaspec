---
alwaysApply: true
---

Never declare types directly in the code, except lib/types

If you’re using inline types, avoid them when the type definition exceeds 20 characters. In such cases, declare the type in lib/types file